# 教练端小程序开发计划

## 一、项目概述

### 1.1 项目目标
为教练员提供移动端签到/划课工具，方便在课堂上快速完成学员出勤记录。

### 1.2 目标用户
- 教练 (coach)
- 可能支持：管理员 (admin)、经理 (manager)

### 1.3 核心功能
- 查看今日排课
- 批量签到/划课
- 查看班级学员
- 个人中心

---

## 二、技术架构

### 2.1 前端技术栈
| 技术 | 说明 |
|------|------|
| 微信小程序 | 原生开发 / uni-app / Taro |
| UI框架 | Vant Weapp / ColorUI |
| 状态管理 | 小程序原生 / MobX |

### 2.2 后端复用
**无需修改后端**，直接复用现有 API：

| 功能 | API | 方法 |
|------|-----|------|
| 登录 | `/api/auth/login` | POST |
| 获取今日排课 | `/api/schedules?startDate=xxx&endDate=xxx` | GET |
| 获取班级学员 | `/api/classes/:id/students` | GET |
| 检查是否已划课 | `/api/lesson-deductions/check/:classId?date=xxx` | GET |
| 创建出勤记录 | `/api/attendances` | POST |
| 更新出勤记录 | `/api/attendances/:id` | PUT |
| 获取用户信息 | `/api/auth/me` | GET |

### 2.3 权限控制
后端已实现教练数据隔离：
```typescript
// coach 角色只能看到自己负责的班级排课
if (role === 'coach') {
  query = query.eq('teacherId', userId);
}
```

---

## 三、功能模块

### 3.1 登录模块

#### 页面：登录页
**路径**: `pages/login/index`

**功能**:
- 账号密码登录
- 登录状态持久化
- 错误提示

**API**:
```javascript
POST /api/auth/login
Request:  { email, password }
Response: { success, data: { token, user } }
```

**UI设计**:
```
┌─────────────────────────┐
│        Logo             │
│                         │
│   ┌─────────────────┐   │
│   │ 邮箱/手机号     │   │
│   └─────────────────┘   │
│   ┌─────────────────┐   │
│   │ 密码            │   │
│   └─────────────────┘   │
│                         │
│   ┌─────────────────┐   │
│   │     登 录       │   │
│   └─────────────────┘   │
│                         │
└─────────────────────────┘
```

---

### 3.2 首页模块

#### 页面：今日课程
**路径**: `pages/index/index`

**功能**:
- 显示今日所有排课
- 按时间排序
- 显示划课状态
- 快速进入划课

**API**:
```javascript
GET /api/schedules?startDate=2026-02-26&endDate=2026-02-26
Response: {
  success,
  data: [
    {
      id, classId, startTime, endTime, status,
      class: { name, code },
      teacher: { name }
    }
  ]
}
```

**UI设计**:
```
┌─────────────────────────┐
│  今日课程 (2月26日)      │
├─────────────────────────┤
│ ┌─────────────────────┐ │
│ │ 09:00 - 10:00       │ │
│ │ 精英班-周一10:00     │ │
│ │ 学员: 8人  已划课 ✓  │ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ 14:00 - 15:00       │ │
│ │ 幼儿班-周一14:00     │ │
│ │ 学员: 10人  未划课   │ │
│ │ [点击划课]           │ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ 19:00 - 20:00       │ │
│ │ 晚间班-周一19:00     │ │
│ │ 学员: 6人  未划课   │ │
│ │ [点击划课]           │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

---

### 3.3 签到模块

#### 页面：学员签到
**路径**: `pages/attendance/index`

**功能**:
- 显示班级学员列表
- 批量设置出勤状态
- 一键全选/取消
- 提交签到记录

**API**:

1. 获取学员列表
```javascript
GET /api/classes/:classId/students
Response: {
  success,
  data: [
    { id, name, phone }
  ]
}
```

2. 检查是否已划课
```javascript
GET /api/lesson-deductions/check/:classId?date=2026-02-26
Response: { success, data: { hasDeducted: boolean } }
```

3. 创建出勤记录
```javascript
POST /api/attendances
Request: {
  studentId: "xxx",
  scheduleId: "xxx",
  classId: "xxx",
  status: "present" | "late" | "absent" | "leave",
  notes: "备注"
}
Response: { success, data: { id } }
```

**UI设计**:
```
┌─────────────────────────┐
│  学员签到 - 精英班       │
│  2026-02-26 14:00       │
├─────────────────────────┤
│ ┌─────────────────────┐ │
│ │ 快捷操作:           │ │
│ │ [全选出勤] [全选缺勤]│ │
│ └─────────────────────┘ │
├─────────────────────────┤
│ ┌─────────────────────┐ │
│ │ ☑ 张小明   [出勤 ▼] │ │
│ │ ☑ 李小红   [出勤 ▼] │ │
│ │ ☐ 王小刚   [缺勤 ▼] │ │
│ │ ☑ 赵小芳   [迟到 ▼] │ │
│ │ ☑ 钱小强   [出勤 ▼] │ │
│ └─────────────────────┘ │
├─────────────────────────┤
│    [ 提交签到记录 ]      │
└─────────────────────────┘

出勤状态选项:
- 出勤 (present)
- 迟到 (late)
- 请假 (leave)
- 缺勤 (absent)
```

---

### 3.4 个人中心模块

#### 页面：我的
**路径**: `pages/mine/index`

**功能**:
- 显示用户信息
- 显示统计数据
- 退出登录

**UI设计**:
```
┌─────────────────────────┐
│                         │
│    [头像]               │
│    张教练               │
│    教练员               │
│                         │
├─────────────────────────┤
│  本月统计               │
│  ┌─────┬─────┬─────┐   │
│  │ 排课 │ 划课 │ 学员 │   │
│  │ 24  │ 180 │ 45  │   │
│  └─────┴─────┴─────┘   │
├─────────────────────────┤
│  [退出登录]             │
└─────────────────────────┘
```

---

## 四、开发计划

### 4.1 阶段一：基础框架 (2-3天)

| 任务 | 工作内容 | 预计时间 |
|------|----------|----------|
| 项目初始化 | 创建小程序项目、配置 | 0.5天 |
| 公共组件 | 导航栏、加载状态、空状态 | 1天 |
| 网络封装 | request 封装、token 处理 | 0.5天 |
| 登录模块 | 登录页、token 存储 | 1天 |

### 4.2 阶段二：核心功能 (3-4天)

| 任务 | 工作内容 | 预计时间 |
|------|----------|----------|
| 首页 | 今日课程列表 | 1天 |
| 签到页面 | 学员列表、状态选择 | 1.5天 |
| 签到提交 | 批量提交出勤记录 | 1天 |
| 错误处理 | 网络异常、重复提交等 | 0.5天 |

### 4.3 阶段三：优化完善 (2天)

| 任务 | 工作内容 | 预计时间 |
|------|----------|----------|
| 个人中心 | 用户信息、统计 | 1天 |
| 交互优化 | 加载动画、操作反馈 | 0.5天 |
| 兼容性测试 | 不同机型测试 | 0.5天 |

---

## 五、技术要点

### 5.1 Token 管理

```javascript
// utils/request.js
const BASE_URL = 'https://your-domain.com/api';

function request(options) {
  const token = wx.getStorageSync('token');

  return wx.request({
    url: BASE_URL + options.url,
    method: options.method || 'GET',
    data: options.data,
    header: {
      'Content-Type': 'application/json',
      'Authorization': token ? `Bearer ${token}` : ''
    },
    success: (res) => {
      // 401 自动跳转登录
      if (res.statusCode === 401) {
        wx.removeStorageSync('token');
        wx.reLaunch({ url: '/pages/login/index' });
        return;
      }
      options.success?.(res.data);
    },
    fail: options.fail
  });
}
```

### 5.2 日期处理

```javascript
// 获取今天的日期范围（UTC格式）
function getTodayRange() {
  const today = new Date();
  const start = new Date(today);
  start.setHours(0, 0, 0, 0);
  const end = new Date(today);
  end.setHours(23, 59, 59, 999);

  return {
    startDate: start.toISOString(),
    endDate: end.toISOString()
  };
}
```

### 5.3 批量提交出勤

```javascript
// 批量提交签到
async function submitAttendance(scheduleId, classId, students) {
  const promises = students.map(student => {
    return request({
      url: '/attendances',
      method: 'POST',
      data: {
        studentId: student.id,
        scheduleId,
        classId,
        status: student.status,
        notes: ''
      }
    });
  });

  return Promise.all(promises);
}
```

---

## 六、注意事项

### 6.1 安全考虑
- Token 存储使用 `wx.setStorageSync`
- 敏感接口需要后端二次验证
- HTTPS 强制要求

### 6.2 用户体验
- 划课成功后显示反馈
- 网络异常时提示重试
- 加载状态展示

### 6.3 数据一致性
- 提交前检查是否已划课
- 避免重复提交
- 失败时允许重新提交

---

## 七、后续扩展

### 7.1 第二期功能
- [ ] 查看历史排课
- [ ] 学员详情查看
- [ ] 请假审批
- [ ] 课程评价

### 7.2 第三期功能
- [ ] 消息推送
- [ ] 数据统计图表
- [ ] 多语言支持

---

## 八、参考资料

- [微信小程序官方文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [Vant Weapp 组件库](https://youzan.github.io/vant-weapp/)
- 后端 API 文档: http://localhost:3000/api-docs
