# 🚀 项目快速参考卡片

**最后更新**: 2024-12-23 | **状态**: 课消收入中心核心功能已完成

---

## 📌 技术栈
- **前端**: React + Vite + Ant Design + TypeScript
- **后端**: MemFire Cloud (PostgreSQL + Auth + REST API)
- **状态管理**: Zustand (`authStore`)

---

## ✅ 已完成功能

### 课消收入中心
| 页面 | 路由 | 功能 |
|------|------|------|
| 课消收入总结 | `/operation/consumption` | 基本盘统计、课单价、班级学员变化监测 |

### 学员管理
| 页面 | 路由 | 功能 |
|------|------|------|
| 学员列表 | `/students` | 搜索、新增（姓名+性别+出生日期+家长电话+班级）、编辑、调班 |
| 流失学员库 | `/students/lost` | 添加流失学员、流失原因（时间问题/生病问题）、召回时间、可召回筛选 |

### 数据统计与分析
| 页面 | 路由 | 功能 |
|------|------|------|
| 班级出勤 | `/attendances` | 时间筛选、教练筛选、出勤率统计、导出Excel |
| 低出勤学员 | `/attendances/continuous-leave` | 时间筛选、教练筛选、连续请假≥2周筛选 |
| 蜜月期客户出勤 | `/attendances/honeymoon` | 新报名30天内学员监控 |

### 班级管理
| 页面 | 路由 | 功能 |
|------|------|------|
| 班级列表 | `/classes` | 新增班级（含负责教练）、低出勤班级筛选、体验课优先排序、排课功能、学员名单 |
| 每周排课 | `/operation/weekly-schedule` | 日历视图和列表视图查看每周排课情况 |

---

## 📁 关键文件

```
frontend/src/
├── services/
│   ├── memfireDB.ts      ← 核心数据服务（所有API调用）
│   └── memfireAuth.ts    ← 认证服务
├── pages/
│   ├── Students.tsx      ← 学员列表
│   ├── LostStudents.tsx  ← 流失学员库
│   ├── Classes.tsx       ← 班级管理
│   ├── ConsumptionAndRevenue.tsx  ← 课消收入总结
│   ├── ClassAttendance.tsx        ← 班级出勤
│   ├── ContinuousLeaveStudents.tsx ← 低出勤学员
│   └── HoneymoonAttendance.tsx    ← 蜜月期出勤
├── components/
│   └── Layout.tsx        ← 侧边栏菜单配置
└── App.tsx               ← 路由配置
```

---

## 🔧 memfireDB.ts 服务模块

| 模块 | 用途 |
|------|------|
| `memfireDB.students` | 学员CRUD、搜索 |
| `memfireDB.classes` | 班级CRUD、学员人数统计 |
| `memfireDB.enrollments` | 报名管理 |
| `memfireDB.attendances` | 出勤统计、低出勤班级/学员 |
| `memfireDB.consumption` | 课消统计、班级变化监测 |
| `memfireDB.honeymoon` | 蜜月期学员查询 |
| `memfireDB.lostStudents` | 流失学员管理、召回 |
| `memfireDB.users` | 教练员列表 |
| `memfireDB.schedules` | 排课管理（含重复排课） |

---

## 📊 数据库表

| 表名 | 用途 |
|------|------|
| `students` | 学员信息（status: active/inactive/graduated）|
| `classes` | 班级信息（teacherId关联负责教练）|
| `enrollments` | 学员-班级关系 |
| `schedules` | 排课记录 |
| `attendances` | 考勤记录 |
| `users` | 用户/教练信息 |

---

## ⚡ 快速启动

```bash
cd frontend
npm run dev
# 访问 http://localhost:5173
# 登录后默认跳转 /operation/consumption
```

---

## 🎯 业务逻辑要点

1. **学员负责教练** = 学员所属班级的负责教练（通过班级关联）
2. **流失学员原因**: 时间问题 / 生病问题（均可设置召回时间）
3. **基本盘变化**: 花名册人数 + 新增 + 召回 - 不续费 - 流失（停卡）
   - **新增**: 成单信息表（payments）中确定新报名的学员（不包括续费）
   - **召回**: 从流失学员库（lost_students）中召回的学员
   - **不续费**: 续费表中确定不续费的学员（renewalStatus = 'no_renewal'）
   - **流失**: 添加到流失学员库的数量
4. **蜜月期**: 首次报名30天内的学员，自动过期
5. **低出勤**: 出勤率<60%，连续缺勤≥2次视为连续请假2周（每周1次课）
6. **调班逻辑**: 
   - 学员调班时，旧班级的 enrollment 状态改为 `transferred`
   - 创建新班级的 `active` enrollment
   - **出勤率计算会累计所有班级**（包括已调班的）的出勤记录
   - 每个班级的应出勤次数从该班级的加入时间开始计算
7. **排课规则保存**：
   - 班级创建排课时，系统自动保存排课规则（重复方式、时间、地点等）
   - 修改排课时自动填充之前的规则，方便调整
   - 修改排课会自动取消该班级所有"待上课"状态的旧排课

---

## ⏳ 待开发功能

- [ ] 现金流中心
- [ ] 工作人员管理
- [ ] 财务管理
- [x] 排课管理（支持每周重复、每日重复）
